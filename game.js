const { StarTwoTone } = require("@material-ui/icons")

// Initialize Kaboom
kaboom({
    global: true,
    fullscreen: true,
    scale: 1,
    debug: true,
})

screen("game", () => {

})

// Start Game
start("game")